# Contributing

Use focused branches and Conventional Commit messages. Run the checks in `docs/testing.md`. Never commit human sequence, credentials, scientific outputs, Nextflow work/cache, or weaken validation to pass. New biological behavior requires an ADR and tests. This is unbranded external software, not nf-core.
