// Configured profile only: the package discovers and independently qualifies
// Apptainer. No Nextflow outer container, so there is no nested container engine.
includeConfig 'canonical.config'
params.canonical_runtime_backend = 'apptainer'
