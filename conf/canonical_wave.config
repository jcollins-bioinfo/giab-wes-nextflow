// CONFIGURED_ONLY. Wave is explicitly disabled; pinned images remain the source
// contract. Worker qualification and separate cloud authorization are required.
includeConfig 'canonical.config'
wave.enabled = false
fusion.enabled = false
params.canonical_execution_environment = 'seqera_wave_configured_only'
