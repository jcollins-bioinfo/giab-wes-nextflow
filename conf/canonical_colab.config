// Existing Apptainer/Singularity first; pinned user-space udocker is the
// no-Docker installation fallback. Qualification is a separate execution gate.
includeConfig 'canonical.config'
params.canonical_runtime_backend = 'auto'
params.canonical_allow_runtime_install = true
