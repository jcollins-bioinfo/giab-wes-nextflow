params.m5_manifest = "${projectDir}/tests/data/m5-generated/manifest.json"
executor.queueSize = 1
process {
    // Host Python launches the pinned, isolated tool containers; no nested Docker.
    withLabel: m5_host { cpus = 2; memory = '4 GB'; time = '15m'; container = null }
}
