// CONFIGURED_ONLY. Runtime/data hydration must be qualified on every worker.
includeConfig 'canonical.config'
process.executor = 'slurm'
executor.queueSize = 1
params.canonical_execution_environment = 'slurm_configured_only'
